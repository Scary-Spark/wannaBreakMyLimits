#include <iostream>
using namespace std;
int mian()
{
    std::string name;
    std::getline(std::cin >> std::ws, name);
    // ws is for removing white spaces, i.e. it's clearing input buffer

    // getline(cin, name);
}