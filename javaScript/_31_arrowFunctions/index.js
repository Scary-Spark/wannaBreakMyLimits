/*
    An Arrow Function is a shorter way to write functions in JavaScript.
    good for simple functions that only used once
*/

const hello = (name) => {
  console.log("hello, " + name);
};
hello("bro");
