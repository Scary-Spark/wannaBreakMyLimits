/*
    = assignment operator
    == comparison operator (compare if values are same)
    === strict equality operator (compare both values & data types)
    != inequality operator
    !== strict inequality operator 
 */
