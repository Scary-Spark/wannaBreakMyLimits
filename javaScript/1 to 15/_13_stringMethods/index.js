let username = "BroCode     ";

let firstChar = username.charAt(0);
let indexOfCo = username.indexOf("Co");
let lastIndexOfO = username.lastIndexOf("o");
let length = username.length();
let trimWhiteSpace = username.trim();
let upperCase = username.toUpperCase();
let lowerCase = username.toLowerCase();
let repeatThreeTimes = username.repeat(3);
let startsWithResult = username.startsWith("Big");
let endWithResult = username.endsWith("de");

let phoneNumber = "123-456-7890";
phoneNumber = phoneNumber.replaceAll("-", " ");
